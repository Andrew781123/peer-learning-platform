import { NextPage } from "next";

type NewAccountPageProps = {};

const NewAccountPage: NextPage = (props: NewAccountPageProps) => {
  const {} = props;

  return <div>New Account</div>;
};

export default NewAccountPage;
